template:
  UUID: #this is uuid section not UUID string
    name: playerName
    f_gui: false #key bind to open gui (0,1) witch is (0:F, 1:Shift+F) default 1
    runnable:
      case: default
      id: 0
      string: ""
    warp_exp: true #(0, 1) witch (0:balance, 1:exp) default 1
player:
